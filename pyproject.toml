[project]
name = "PyPI"
description = "Python Package Indexs"
dependencies = ["requests"]
dynamic = ["version", "readme", "optional-dependencies"]

[tool.flake8]
max-line-length = 230

[tool.black]
line-length = 230
